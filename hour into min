#include <stdio.h>

int main()
{
    int hours;
    int minutes;

    printf("Enter time in hours: ");
    scanf("%d", &hours);

    minutes = hours * 60;

    printf("%d hour is equal to %d minute.", hours, minutes);

    return 0;
}
