#include <stdio.h>
int main()
{
    float a, b;
    printf("enter a");
    scanf("%d", &a);

    printf("enter b");
    scanf("%d", &b);

    float c = a / b;
    printf("c is : %f", c);
    return 0;
}
